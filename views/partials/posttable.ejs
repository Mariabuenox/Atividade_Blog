<%- console.log(JSON.stringify(dados))%> 
 <%- console.log(JSON.stringify(req.session))%>  
<center>
 <table border="1px"> 
    <thead>
    <tr>
        <td>id</td>
        <td>id_users</td>
        <td>titulo</td>
        <td>conteudo</td>
        <td>data_criacao</td>
        <td>ações</td>
    </tr>
 </thead> 
         <% let i; for (i=0; i<dados.length; i++) {%> 
            <% if (dados [i].username==req.session.username) {%>  
            <tr>
                 <% } %>
                <td><%= dados[i].id %></td>
                <td><%= dados[i].id_users %></td>
                <td><%= dados[i].titulo %></td>
                <td><%= dados[i].conteudo %></td>
                <td><%= dados[i].data_criacao %></td>
                <td><a href="/deleteP/<%= dados[i].id%>" onclick= "return confirm(' Tem certeza que deseja deletar este post? Esta ação não pode ser desfeita!')">Deletar</a></td   >
                
            </tr>      
        <% } %>
    </tbody>
    <tfoot>
        <tr><th colspan="3">
            <%= dados.length %>
        </th></tr>
    </tfoot>
</table>
</center>
<br>
<br>

